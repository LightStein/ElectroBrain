# ── secrets ──────────────────────────────────────────────────────────────────
secrets.env
*.pem
*.key
id_ed25519*

# ── runtime state (lives on George's laptop, never in the repo) ──────────────
raw/
work/
index/
state/
logs/
gateway/run/
gateway/uploads/
bot/uploads/
bot/.progress

# ── deps / build ─────────────────────────────────────────────────────────────
node_modules/
package-lock.json
__pycache__/
*.pyc

# editor scratch (can contain secrets from an in-progress edit)
*.swp
*.swo
*~
